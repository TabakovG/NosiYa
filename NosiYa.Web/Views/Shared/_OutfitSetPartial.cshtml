@model OutfitSetAllViewModel

<div class="col-md-4">
	<div class="card mb-3">
		<img class="card-img-top" src="@Model.ImageUrl" alt="Outfit Image">
		<div class="card-body text-center">
			<h4>@Model.Name</h4>
			<br />
			<a asp-controller="OutfitSet" asp-action="Details" asp-route-id="@Model.Id"
			   class="btn btn-success">Детайли</a>
			@if (this.User.Identity?.IsAuthenticated ?? false)
			{
				<a asp-controller="OutfitSet" asp-action="Edit" asp-route-id="@Model.Id"
				   class="btn btn-warning">Промени</a>
				<a asp-controller="OutfitSet" asp-action="Delete" asp-route-id="@Model.Id"
				   class="btn btn-danger">Изтрий</a>
				<p></p>
			}
			<a asp-controller="Rent" asp-action="Add" asp-route-id="@Model.Id"
			   class="btn btn-success">Резервирай</a>
		</div>
	</div>
</div>

